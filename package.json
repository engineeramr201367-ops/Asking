{
  "name": "super-run",
  "version": "1.0.0",
  "description": "Super Run - Mario-style platformer packaged for Android/iOS with Capacitor",
  "private": true,
  "scripts": {
    "copy:web": "node scripts/copy-web.js",
    "sync": "npm run copy:web && npx cap sync",
    "add:android": "npm run copy:web && npx cap add android",
    "add:ios": "npm run copy:web && npx cap add ios",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor-community/admob": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
